---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10
build:
  requirementsPath: requirements.txt
run:
  scriptName: wsgi.py
  persistenceMount: /data
  command: gunicorn --bind 0.0.0.0:8080 app:wsgi
  containerPort: 8080
